// middleware/trailing-slash.global.js - COMPLETELY DISABLED
// All trailing slash handling is now done via nuxt.config.ts routeRules

export default defineNuxtRouteMiddleware(() => {
  // Completely disabled - routeRules handle all redirects
})
