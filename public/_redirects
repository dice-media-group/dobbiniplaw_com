# Netlify redirects - SIMPLIFIED to avoid conflicts with Nuxt middleware
# Only handle specific cases that might not be caught by Nuxt

# Handle any server-level issues
/home  /  301
/index /  301

# Let Nuxt handle all other redirects to avoid conflicts
