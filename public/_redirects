# Netlify redirects - STANDARDIZED URL structure without trailing slashes

# Handle any server-level issues
/home  /  301
/index /  301

# Remove trailing slashes from all URLs except root
/*/ /:splat 301!
